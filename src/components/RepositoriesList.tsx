import { Input } from '@material-ui/core';

const RepositoriesList :React.FC = () => {
    return (
        <form>
            <Input/>
            <button>Add</button>
        </form>
    )
       
    
}

export default RepositoriesList;
